{
  "buildCommand": "mkdir -p backend/assets && curl -fsSL 'https://cdn.jsdelivr.net/gh/dejavu-fonts/dejavu-fonts@2.37/ttf/DejaVuSans.ttf' -o backend/assets/DejaVuSans.ttf && cd frontend && npm run build",
  "installCommand": "rm -rf node_modules frontend/node_modules && npm install --include=optional",
  "outputDirectory": "dist",
  "env": {
    "UV_PYTHON": "3.12",
    "PYO3_USE_ABI3_FORWARD_COMPATIBILITY": "1"
  },
  "functions": {
    "api/index.py": { "maxDuration": 60 }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.py" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}

